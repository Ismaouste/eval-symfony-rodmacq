{% extends 'base.html.twig' %}

{% block title %}Entreprise{% endblock %}

{% block body %}
    <h1>Entreprise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ company.name }}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ company.siret }}</td>
            </tr>
            <tr>
                <th>Addresse</th>
                <td>{{ company.address }}</td>
            </tr>
            <tr>
                <th>Code Postal</th>
                <td>{{ company.zipCode }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ company.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('company_index') }}" class="btn btn-primary">Retourner à la liste</a>

    <a href="{{ path('company_edit', {'id': company.id}) }}" class="btn btn-success">Editer</a>

    {{ include('company/_delete_form.html.twig') }}
{% endblock %}
